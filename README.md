
  # Transport Management System UI

  This is a code bundle for Transport Management System UI. The original project is available at https://www.figma.com/design/eUnGwiS2Zw7NuPFGChWrpk/Transport-Management-System-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  